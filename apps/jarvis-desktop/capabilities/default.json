{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Jarvis desktop default window permissions",
  "windows": ["main"],
  "permissions": ["core:default"]
}
