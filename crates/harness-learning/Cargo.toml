[package]
name = "harness-learning"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Self-improving-agent surface — long-term learning value types (skill usage telemetry, lifecycle) and their persistence trait surfaces. Extracted from harness-core so the agent loop stays focused. Phase 0 (telemetry-only) of `docs/proposals/self-improving-agent.zh-CN.md`."

[dependencies]
async-trait.workspace = true
regex.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
