{
  "name": "@jarvis/skill",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Anthropic-style Skills catalog — SKILL.md (markdown + YAML frontmatter) discovery + activation selectors + store.",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jarvis/core": "workspace:*",
    "@jarvis/store": "workspace:*",
    "yaml": "^2.6.0"
  }
}
