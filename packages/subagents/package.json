{
  "name": "@jarvis/subagents",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SubAgent trait + registry for delegated agents (internal loop or CLI sidecar) invoked via subagent.<name> tools, + runs ledger.",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jarvis/core": "workspace:*",
    "@jarvis/store": "workspace:*"
  }
}
