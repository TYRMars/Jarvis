{
  "name": "@jarvis/agent-profile",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent identity bundles (name / provider / model / system prompt) + AgentProfileStore.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jarvis/core": "workspace:*",
    "@jarvis/store": "workspace:*"
  }
}
