{
  "name": "@jarvis/channel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Channel value types (instance/binding/inbound/outbound) + ChannelDispatcher / *Store traits + JsonFile & Memory store backends. Per-kind adapters, inbound signature verification, and OAuth live in the server (a later phase).",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jarvis/core": "workspace:*",
    "@jarvis/shared-types": "workspace:*",
    "@jarvis/store": "workspace:*"
  }
}
