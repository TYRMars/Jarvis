{
  "name": "@jarvis/automation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AutomationTask + ScheduleSpec (one-time / interval) + AutomationStore (JSON-file + in-memory) for scheduled work. Ported from harness-automation.",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jarvis/core": "workspace:*",
    "@jarvis/store": "workspace:*"
  }
}
